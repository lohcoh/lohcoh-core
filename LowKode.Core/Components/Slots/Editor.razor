@namespace LowKode.Core.Components
@inject ILowKodeComponentService lkComponents
@EditorComponent
@code {

    [Parameter] public Type TModel { get; set; }
    [Parameter] public IItem Value { get; set; }

    ComponentBase EditorComponent;

    protected override void OnInitialized()
    {
        EditorComponent = lkComponents.Create(Type componentType, Type modelType, PropertyInfo property);
    }

}
