@namespace LowKode.Core.Components
@inject ILowKodeComponentService lkComponents
@typeparam TItem
@EditorComponent
@code {

    [Parameter]
    public Type TModel { get; set; }
    ComponentBase EditorComponent;

    protected override void OnInitialized()
    {
        EditorComponent = lkComponents.Create(ComponentBase:<Editor<TItem>>();
    }

}
