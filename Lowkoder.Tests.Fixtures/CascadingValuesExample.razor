@using System;
@using Microsoft.AspNetCore.Components.Forms
@using LowKode.Core.Components
@using Microsoft.AspNetCore.Components.Rendering;


@namespace LowKode.Tests.Fixtures

@foreach (var contextValue in contextValues)
{
    <CascadingValue Value="@contextValue">
        <CascadingValuesLabel/>
    </CascadingValue>
}

@code {
    string[] contextValues = new string[] { "context-value-one", "context-value-two", "context-value-three"  };
}